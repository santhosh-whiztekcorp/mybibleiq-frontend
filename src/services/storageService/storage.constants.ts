import { app } from "@/constants/app";

/* ---- Keys ---- */
export const ACCESS_TOKEN_KEY = app.storageKeys.accessToken || "accessToken";
export const REFRESH_TOKEN_KEY = app.storageKeys.refreshToken || "refreshToken";
export const USER_INFO_KEY = app.storageKeys.userInfo || "userInfo";
